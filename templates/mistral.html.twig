<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Hackathon</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
<ul>

</ul>

liste des patient :

<ul>
    {% for patient in patients %}
        <li>{{ patient.name }} - <a href="{{ path('chatbot_admin', {id: patient.id}) }}">Chatbot (partie admin)</a> - <a href="{{ path('chatbot_patient', {id: patient.id}) }}">Chatbot (partie patient)</a></li>
    {% endfor %}
</ul>

{#    <form action="{{ url('upload') }}" method="post" enctype="multipart/form-data">#}
{#        <textarea name="question" id="" cols="30" rows="10" placeholder="Message du médecin"></textarea>#}
{#        <input type="file" name="file">#}
{#        <input type="submit" value="Upload">#}
{#    </form>#}
</body>
</html>
